%table.table.table-bordered
  %thead.thead-default
    %tr
      %th
        = t('admin.trains.list')
      %th{:colspan => "3"}
  %tbody
    = render @trains
%br/
= link_to t('admin.trains.new.title'), new_admin_train_path
